# Defaults for `docker compose` / CI image builds only (no secrets).
# The real file bundled as an asset is `.env`, created in Dockerfile from this file.
# Flutter web without API_BASE_URL uses http://<page-host>:8080 — fine for our compose (web :80, API :8080).
API_BASE_URL=http://localhost:8080
