# EXAMPLE ONLY — copy to `.env` for local use (`.env` is gitignored).
#
#   cp .env.example .env
#
# Local:
API_BASE_URL=http://localhost:8080
#
# Production (Vercel): use same-origin — vercel.json proxies /api and /auth to Render.
# API_BASE_URL=same-origin
