# EXAMPLE ONLY — copy to `.env` for local use (`.env` is gitignored).
#
#   cp .env.example .env
#
# Local:
API_BASE_URL=http://localhost:8080
#
# Production (set in Vercel dashboard, NOT in this file):
# API_BASE_URL=https://novora-api.onrender.com
