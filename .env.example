# Copy to .env (Flutter loads this at startup).
# Use http://localhost:8080 when you use `flutter run` on the web with the API on the same machine
# (matches the browser tab host and works with Docker: ./scripts/run-api-docker.sh).
API_BASE_URL=http://localhost:8080
